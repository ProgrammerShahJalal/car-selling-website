import { styled } from "@mui/system";
import Button from '@mui/material/Button';

const MuiButton = styled(Button)({
    background: 'linear-gradient(45deg,darkcyan,orange)',
    color: '#fff',
    padding: '10px 15px'
})

export default MuiButton;